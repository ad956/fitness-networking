import { Routes, Route } from "react-router-dom";

const PartnerRoutes = () => (
  <Routes>
    <Route path="/" element={<p>Index page of Partner</p>} />
  </Routes>
);

export default PartnerRoutes;
